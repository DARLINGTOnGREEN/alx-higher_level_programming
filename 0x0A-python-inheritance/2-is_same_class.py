#!/usr/bin/python3
"""Module that define a function"""


def is_same_class(obj, a_class):
    """function called is_same_class(obj, a_class)
    return true if obj is a class else
    false
    """
    return True if type(obj) is a_class else False
